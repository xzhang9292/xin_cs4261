<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shuangke.toolbar">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MimiApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".All_categories" />
        <activity
            android:name=".signIn"
            android:label="Sign In"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".userProjectInfor"
            android:label="My projects"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PostProject"
            android:label="@string/title_activity_post_project"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>